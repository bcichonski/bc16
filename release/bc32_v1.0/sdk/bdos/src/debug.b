byte main()
{
    word a;

    //mfill(0xf000, 0xff, 0xab);
    
    //memcpy(0xf000, 0xe000, 0xff);

    //a <- "this is test string";

    //strncpy(a, 0xe000, 0xff);

    a <- 0;
    a <- a + 1;
}
